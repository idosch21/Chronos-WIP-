databases:
  - name: chronos-database
    plan: free
    postgresMajorVersion: "15"

services:
  - type: web
    name: chronos-backend
    env: docker
    plan: free
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: chronos-database
          property: connectionString